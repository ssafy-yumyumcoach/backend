<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.yumyumcoach.domain.ai.chatbot.mapper.AiChatConversationMapper">
    <resultMap id="ConversationMap" type="com.yumyumcoach.domain.ai.chatbot.entity.AiChatConversation">
        <id column="id" property="id"/>
        <result column="email" property="email"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <insert id="insertConversation" parameterType="map" useGeneratedKeys="true" keyProperty="conversation.id">
        INSERT INTO ai_chat_conversations (email)
        VALUES (#{conversation.email})
    </insert>

    <select id="findByIdAndEmail" parameterType="map" resultMap="ConversationMap">
        SELECT *
        FROM ai_chat_conversations
        WHERE id = #{conversationId}
          AND email = #{email}
    </select>
</mapper>
